namespace SimpleConnections.model
{
    public enum PAGETYPE
    {
        MAINMENU, CONNECTIONS, PROFILE, CHATROOM
    }
    public enum ART
    {
        EXIT, APPLE, CONSOLE, PC, USB, TITLE, COLLECTIONS,
        PROFILE,
        CHATROOM
    }
    public enum ButtonType
    {
        Exit, Connections, Profile, ChatRoom, Apple, Console, Pc, Usb,
        MainMenu
    }
}